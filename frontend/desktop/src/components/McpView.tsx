import type { ReactNode } from "react";

export function McpView(): ReactNode {
  return (
    <div className="flex h-full items-center justify-center bg-white p-6 text-sm text-slate-700">
      MCP view scaffolded. Connector-level actions land in a follow-up slice.
    </div>
  );
}

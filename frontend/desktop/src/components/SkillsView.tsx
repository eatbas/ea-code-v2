import type { ReactNode } from "react";

export function SkillsView(): ReactNode {
  return (
    <div className="flex h-full items-center justify-center bg-white p-6 text-sm text-slate-700">
      Skills view scaffolded. Full skill CRUD wiring lands in a follow-up slice.
    </div>
  );
}
